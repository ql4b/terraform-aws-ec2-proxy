#!/bin/bash
# SessionStart hook: ask the agent to propose next tasks
# Steering files (CURRENT_STATE.md, ENHANCEMENTS.md) are already in context.

echo "Based on CURRENT_STATE.md and ENHANCEMENTS.md (already in context), propose 2-3 next tasks to work on. Prioritize by P-level, picking the highest-impact unchecked items. Present them as numbered options with a brief rationale for each, and ask which one to proceed with."
